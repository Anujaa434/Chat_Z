import React from "react";

const ChatHeader = ({ activeChat, model, setModel }) => {
  // Header removed - title is in sidebar, model selector moved to input area
  return null;
};

export default ChatHeader;
